import React from 'react';
import PlainHexagon from '/src/components/PlainHexagon';

const LoadingBar = () => {

    return (
        <div className="loading-bar">
            <PlainHexagon size={80}/>
        </div>
    );
};

export default LoadingBar;